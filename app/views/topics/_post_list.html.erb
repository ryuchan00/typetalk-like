<ul class="media-list">
  <% if post_data.empty? %>
    <p>検索条件に合うものがありません。</p>
  <% else %>
    <% post_data.each do |post| %>
        <% user = post['name'] %>
        <li class="media">
          <div class="panel panel-default">
            <div class="panel-heading">
              <h1 class="panel-title"><%= user %></h1>
            </div>
            <div class="panel-body">
          <div class="media-left">
            <img class="media-object img-rounded" src="<%= post['imageUrl'] %>" alt="" height="50" width="50">
          </div>
          <div class="media-body">
            <div>
              <p><%= link_to 'typetalkで見る', "https://typetalk.in/topics/#{post['topic_id']}/posts/#{post['post_id']}", :target => ["_blank"] %></p>
              <span class="text-muted">投稿時間 <%= post['created_at'] %></span>
            </div>
            <div>
              <%= simple_format(post['message']) %>
            </div>
            <div>
              <p class="col-xs-3 bg-danger">️イイネの数❤︎️ <%= post['like'] %></p>
            </div>
          </div>
          </div>
          </div>
        </li>
    <% end %>
  <% end %>
</ul>